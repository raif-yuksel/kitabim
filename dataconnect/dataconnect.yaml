specVersion: "v1"
serviceId: "y"
location: "europe-central2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "y-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
